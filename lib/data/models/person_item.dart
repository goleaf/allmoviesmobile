class PersonItem {
  final String name;
  final String knownFor;
  final String biography;

  const PersonItem({
    required this.name,
    required this.knownFor,
    required this.biography,
  });
}
